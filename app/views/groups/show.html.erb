<h2><%= @group.name %></h2>

	
	<%= image_tag group_img(@group.icon.url), width: '100%' %>
	
	<%= link_to 'Edit', edit_group_path(@group)%>
	<%= link_to 'Back', expenses_path%>


	<p>
  	Amount: <%= @expense.amount %> |
		Date: <%= @expense.createdAt %>		
	</p>