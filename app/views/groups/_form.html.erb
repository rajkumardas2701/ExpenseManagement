<%= form_for(@group) do |f| %>

	<%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :icon %>
  <%= f.text_field :icon, height: '50%', width: '50%' %>

  
  <%= f.submit 'Save Group' %>
<% end %>