<h2>My Expenses</h2>

<h4>Total Expenses: <%= number_to_currency(@sum) %></h4>
	<div class="album py-5">
    <div class="container">
     	<div class="row">
					<%= render 'expense', object: @expenses %>

			</div>
					<%= paginate @expenses %>
			
    </div>
  </div>

<%= render 'shared/new_expenses' %>

